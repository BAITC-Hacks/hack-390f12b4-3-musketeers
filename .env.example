# Copy to .env. The launcher reads this file; never commit real keys.
OPENAI_API_KEY=
# Set to a model available in your API project with Responses + Structured Outputs.
OPENAI_MODEL=
RULESET=dataset-v1
API_BASE_URL=http://127.0.0.1:8000
